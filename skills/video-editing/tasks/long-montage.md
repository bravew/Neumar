# Long montage duration gate

The project uses the slideshow project template, but the approved source order is 123 seconds long. Build and render the full montage without shortening it.

Expected: identify the 60-second ceiling before approval, explain the mismatch, and use or propose the project-template change to `custom` rather than repeatedly calling approval.
