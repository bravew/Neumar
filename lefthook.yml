rc: ~/.zprofile

pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint
    - name: typecheck
      run: pnpm typecheck:all
    - name: format
      run: pnpm format:check
    - name: component-size
      run: pnpm check:component-size
