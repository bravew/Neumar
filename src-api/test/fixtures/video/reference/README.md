# Video reference fixtures

Synthetic clips for Analyze Video tests. See [LICENSE.md](LICENSE.md).
Regenerate with `python3 generate.py` from this directory (requires ffmpeg
and, for speech beds, macOS `say`).

Each `*.mp4` has a sibling `*.cuts.json` with hand-marked hard cuts in
milliseconds. Speech clips also have `*.transcript.txt`.
